# my-suckless-builds

## Installation
* run `sudo make install` for the components that you want.
* if you are using my dwm config, for some binds to work you must `cp -r ./.wm-scripts/ ~/` and set the appropriate path in the VOLMAN_PATH macro within `./dwm/config.def.h`
